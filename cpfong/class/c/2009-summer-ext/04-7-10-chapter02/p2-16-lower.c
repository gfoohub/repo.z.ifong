#include <stdio.h>

int lower(int);

main(){
	int 
	char a[4];

	a[0]='1';a[1]='2';a[2]='\0';
	printf("a is %d\n", atoi(a));
}

int lower(int i){

	if (c >= 'A' && c <= 'Z') return c + 'a' - 'A';
	else return c;
}
